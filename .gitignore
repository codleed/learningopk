# Worktrees / agent scratch
.worktrees/
.opencode/plans/
settings.json

# Secrets & local env — NEVER commit (.env.example files stay tracked)
**/.env
**/.env.local
**/.env.*.local
*.pem
*.key

# Temp & build artifacts
.tmp-*
*.tsbuildinfo
