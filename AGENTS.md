# learningopk — agent notes

The pnpm app and its build/test commands are documented in [`learningopk/AGENTS.md`](learningopk/AGENTS.md).

## Agent skills

### Issue tracker

Issues live in this repo's GitHub Issues (`codleed/learningopk`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles use their default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
